# Dependencies for Signal Bot
# Core dependencies for bot functionality

# QR code generation (used during device linking setup)
qrcode[pil]==7.4.2
Pillow>=8.0.0

# AI integration dependencies
google-generativeai>=0.3.0
requests>=2.28.0

# Markdown processing for AI responses
markdown>=3.4.0

# Note: Install in virtual environment:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt